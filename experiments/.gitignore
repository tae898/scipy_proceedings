# generated / downloaded artifacts — not committed
results/
datasets/raw/
datasets/prepared/
__pycache__/
*.pyc

# Conceptual figures (architecture, hybrid_workflow) ARE committed — they're paper assets.
# Only the retired result bar-charts are ignored (results go in tables, not figures).
figures/fig_*.png
figures/fig_*.svg
